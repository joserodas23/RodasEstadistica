% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grafico_circular.R
\name{grafico_circular}
\alias{grafico_circular}
\title{Crea gráficos circulares limpios para variables cualitativas}
\usage{
grafico_circular(
  datos,
  ordenar = "frecuencia",
  decrecer = TRUE,
  colores = NULL,
  titulo_grafico = "Distribución de Frecuencias",
  subtitulo_grafico = NULL,
  caption_grafico = NULL,
  mostrar_valores = TRUE,
  mostrar_porcentaje = TRUE,
  mostrar_leyenda = TRUE,
  posicion_leyenda = "right",
  tamano_texto_valores = 3.5,
  tamano_texto_leyenda = 10,
  tamano_texto_titulo = 14,
  minimo_porcentaje_etiquetas = 5,
  etiquetas_con_caja = FALSE,
  relacion_circular = 1
)
}
\arguments{
\item{datos}{Vector con los datos categóricos a analizar}

\item{ordenar}{Método para ordenar las categorías: "ninguno", "alfabetico", "frecuencia" (por defecto "frecuencia")}

\item{decrecer}{Lógico. Si TRUE y ordenar="frecuencia", ordena de mayor a menor frecuencia (por defecto TRUE)}

\item{colores}{Vector de colores para los sectores o un nombre de paleta de RColorBrewer}

\item{titulo_grafico}{Título para el gráfico (por defecto "Distribución de Frecuencias")}

\item{subtitulo_grafico}{Subtítulo para el gráfico (por defecto NULL)}

\item{caption_grafico}{Caption (pie de gráfico) para el gráfico (por defecto NULL)}

\item{mostrar_valores}{Lógico. Si TRUE, muestra valores dentro de los sectores (por defecto TRUE)}

\item{mostrar_porcentaje}{Lógico. Si TRUE, muestra porcentajes en las etiquetas (por defecto TRUE)}

\item{mostrar_leyenda}{Lógico. Si TRUE, muestra la leyenda (por defecto TRUE)}

\item{posicion_leyenda}{Posición de la leyenda: "right", "left", "bottom", "top" (por defecto "right")}

\item{tamano_texto_valores}{Tamaño del texto para los valores en los sectores (por defecto 3.5)}

\item{tamano_texto_leyenda}{Tamaño del texto en la leyenda (por defecto 10)}

\item{tamano_texto_titulo}{Tamaño del texto del título (por defecto 14)}

\item{minimo_porcentaje_etiquetas}{Porcentaje mínimo para mostrar etiquetas (por defecto 5)}

\item{etiquetas_con_caja}{Lógico. Si TRUE, muestra etiquetas con fondo (por defecto FALSE)}

\item{relacion_circular}{Relación de aspecto del gráfico, valores < 1 lo hacen más ancho (por defecto 1)}
}
\value{
Un objeto ggplot2 que puede ser modificado con funciones adicionales de ggplot2
}
\description{
Esta función genera gráficos circulares (pie charts) para variables cualitativas o categóricas,
sin bordes ni cuadros alrededor, mostrando porcentajes y frecuencias absolutas.
}
\examples{
# Datos de ejemplo
especies <- c("Anchoveta", "Caballa", "Jurel", "Merluza", "Sardina",
             "Anchoveta", "Caballa", "Jurel", "Merluza", "Sardina")

# Gráfico circular básico
grafico_circular_limpio(especies)

# Gráfico circular con personalización
colores_personalizados <- c("Anchoveta" = "#66c2a5",
                           "Caballa" = "#fc8d62",
                           "Jurel" = "#8da0cb",
                           "Merluza" = "#e78ac3",
                           "Sardina" = "#a6d854")
grafico_circular_limpio(
  especies,
  titulo_grafico = "Distribución de Especies Capturadas",
  subtitulo_grafico = "Figura 02 - Datos de captura pesquera",
  colores = colores_personalizados,
  posicion_leyenda = "right",
  tamano_texto_valores = 4
)

}
