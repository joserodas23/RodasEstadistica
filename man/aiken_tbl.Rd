% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aiken.R
\name{aiken_tbl}
\alias{aiken_tbl}
\title{V de Aiken en formato "tidy" (por ítem/criterio y global)}
\usage{
aiken_tbl(
  data,
  item_col,
  judge_col,
  rating_cols,
  lo = 1,
  hi = 4,
  global = TRUE,
  global_method = c("concat", "mean"),
  cut_keep = 0.8,
  cut_review = 0.7,
  lab_keep = "Mantener",
  lab_review = "Revisar",
  lab_rewrite = "Reescribir/Eliminar"
)
}
\arguments{
\item{data}{Data frame largo: una fila = un juez valorando un ítem.}

\item{item_col, judge_col}{Columna identificadora de ítem y de juez. Acepta
\strong{bare names} (p. ej., \code{item_id}) o \strong{strings} (p. ej., \code{"item_id"}).}

\item{rating_cols}{Vector de nombres de columnas de criterios (p. ej.,
\code{c("relevancia","claridad","coherencia")}).}

\item{lo, hi}{Límites inferior y superior de la escala (default 1 y 4).}

\item{global}{Logical. Si \code{TRUE}, añade V global por ítem.}

\item{global_method}{\code{"concat"} (default) concatena todas las valoraciones de
los criterios para V global; \code{"mean"} promedia las V por criterio.}

\item{cut_keep, cut_review}{Umbrales para la decisión (default 0.80 y 0.70).}

\item{lab_keep, lab_review, lab_rewrite}{Etiquetas para la decisión.}
}
\value{
Tibble con: \code{n_jueces}, \code{V_<criterio>}, \code{dec_V_<criterio>},
y, si \code{global=TRUE}, \code{V_global} y \code{dec_global}.
}
\description{
Calcula la V de Aiken por ítem y por cada criterio (p. ej., relevancia, claridad,
coherencia) a partir de un data frame en formato largo (una fila = un juez por ítem).
También calcula una V global por ítem y su decisión (Mantener/Revisar/Reescribir),
con umbrales personalizables.
}
\examples{
# aiken_tbl(df, item_col = item_id, judge_col = judge_id,
#           rating_cols = c("relevancia","claridad","coherencia"),
#           lo = 1, hi = 4, global = TRUE)
}
