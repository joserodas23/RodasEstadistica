% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aiken.R
\name{aiken_report}
\alias{aiken_report}
\title{Reporte bonito de V de Aiken (tabla con colores y exportación)}
\usage{
aiken_report(
  data,
  item_col = NULL,
  judge_col = NULL,
  rating_cols = NULL,
  lo = 1,
  hi = 4,
  keep_cols = c("item_text", "dimension"),
  group_col = NULL,
  cut_keep = 0.8,
  cut_review = 0.7,
  lab_keep = "Mantener",
  lab_review = "Revisar",
  lab_rewrite = "Reescribir/Eliminar",
  digits = 2,
  title = "Validación de contenido – V de Aiken",
  subtitle = "Panel de expertos",
  output = NULL
)
}
\arguments{
\item{data}{Data frame crudo (formato largo) o con columnas V_* ya calculadas.}

\item{item_col, judge_col}{Columnas identificadoras (bare o string). Requeridas si hay que calcular V.}

\item{rating_cols}{Vector con columnas de criterios. Requerido si hay que calcular V.}

\item{lo, hi}{Escala mínima y máxima (default 1 y 4).}

\item{keep_cols}{Columnas extra a mostrar (p. ej., \code{c("item_text","dimension")}).}

\item{group_col}{Columna para agrupar visualmente (default "dimension" si existe).}

\item{cut_keep, cut_review, lab_keep, lab_review, lab_rewrite}{Umbrales y etiquetas.}

\item{digits}{Decimales en columnas V (default 2).}

\item{title, subtitle}{Título/subtítulo de la tabla.}

\item{output}{Ruta de salida opcional. Extensión define formato:
\code{.html} / \code{.png} usan \pkg{gt}; \code{.docx} usa \pkg{flextable}.}
}
\value{
(invisible) lista con \code{table} (objeto gt/flextable), \code{data}
y \code{path} (si se guardó).
}
\description{
Genera una tabla formateada a partir de valoraciones de expertos y del cálculo
de V de Aiken por ítem (por criterio y global). Si \code{data} ya trae columnas
\code{V_*}, se usan esas; si no, se calcula V con \code{aiken_tbl()}.
}
\details{
Requiere tener instalados: \pkg{gt}, \pkg{scales} (si HTML/PNG) y
opcionalmente \pkg{webshot2} (para PNG) o \pkg{flextable}+\pkg{officer} (para DOCX).
}
\examples{
# aiken_report(df, item_col = item_id, judge_col = judge_id,
#   rating_cols = c("relevancia","claridad","coherencia"),
#   keep_cols = c("item_text","dimension"),
#   output = "F:/Validacion de intrumentos/tabla_aiken.html")
}
