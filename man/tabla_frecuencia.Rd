% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tabla_frecuencia.R
\name{tabla_frecuencia}
\alias{tabla_frecuencia}
\title{Crea una tabla de frecuencias con intervalos de clase}
\usage{
tabla_frecuencia(
  datos,
  k = 5,
  digitos = 2,
  metodo = "sturges",
  breaks = NULL,
  mostrar_tabla = TRUE,
  color_encabezado = "steelblue",
  grafico = "ninguno",
  color_grafico = "steelblue",
  titulo_grafico = NULL,
  subtitulo_grafico = NULL,
  caption_grafico = NULL,
  eje_x_nombre = NULL,
  eje_y_nombre = NULL,
  intervalo_cerrado = FALSE,
  labels = NULL,
  formato_tabla = "html"
)
}
\arguments{
\item{datos}{Vector numérico con los datos a analizar}

\item{k}{Número de intervalos o clases deseados (por defecto 5)}

\item{digitos}{Número de decimales a mostrar en los resultados (por defecto 2)}

\item{metodo}{Método para calcular los breaks ("sturges", "fd", "scott" o "manual")}

\item{breaks}{Vector de puntos de corte (solo si metodo="manual")}

\item{mostrar_tabla}{Lógico. Si TRUE, muestra la tabla formateada con kableExtra}

\item{color_encabezado}{Color para el encabezado de la tabla (por defecto "steelblue")}

\item{grafico}{Tipo de gráfico a generar: "ninguno", "histograma", "poligono", "ojiva", o "todos"}

\item{color_grafico}{Color principal para los gráficos (por defecto "steelblue")}

\item{titulo_grafico}{Título para los gráficos (por defecto NULL, usa un título genérico)}

\item{subtitulo_grafico}{Subtítulo para los gráficos (por defecto NULL)}

\item{caption_grafico}{Caption (pie de gráfico) para los gráficos (por defecto NULL)}

\item{eje_x_nombre}{Nombre personalizado para el eje X (por defecto NULL, usa nombres genéricos)}

\item{eje_y_nombre}{Nombre personalizado para el eje Y (por defecto NULL, usa nombres genéricos)}

\item{intervalo_cerrado}{Lógico. Si TRUE, usa formato \link{a b} para intervalos cerrados en ambos extremos}

\item{labels}{Vector de caracteres con etiquetas personalizadas para los intervalos}

\item{formato_tabla}{Formato para la tabla: "html", "markdown", "latex" o "simple" (por defecto "html")}
}
\value{
Una lista con dos elementos: tabla (data frame con la tabla de frecuencias) y graficos (lista de objetos ggplot si se solicitaron)
}
\description{
Esta función genera una tabla de frecuencias completa para una variable continua,
incluyendo frecuencias absolutas, relativas, acumuladas y marcas de clase.
}
\examples{
# Datos de ejemplo
datos_ejemplo <- rnorm(100, mean = 50, sd = 10)

# Tabla básica con 5 intervalos
tabla_frecuencia(datos_ejemplo)

# Tabla con 7 intervalos y 3 decimales
tabla_frecuencia(datos_ejemplo, k = 7, digitos = 3)

# Usar método de Freedman-Diaconis para calcular intervalos
tabla_frecuencia(datos_ejemplo, metodo = "fd")

# Definir puntos de corte manualmente
tabla_frecuencia(datos_ejemplo, metodo = "manual",
                 breaks = c(20, 30, 40, 50, 60, 70, 80))

# Generar tabla con histograma
tabla_frecuencia(datos_ejemplo, grafico = "histograma")

# Generar todos los gráficos con colores personalizados
tabla_frecuencia(datos_ejemplo, grafico = "todos",
                 color_grafico = "darkblue",
                 titulo_grafico = "Análisis de mi variable")

# Usar intervalos cerrados en ambos extremos [a b]
tabla_frecuencia(datos_ejemplo, intervalo_cerrado = TRUE)

# Usar etiquetas personalizadas
tabla_frecuencia(datos_ejemplo, metodo = "manual",
                 breaks = c(20, 30, 40, 50, 60, 70, 80),
                 labels = c("[0 8]", "[9 15]", "[16 25]"))

}
