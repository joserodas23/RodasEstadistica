% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tabla_cuantitativa.R
\name{tabla_cuantitativa}
\alias{tabla_cuantitativa}
\title{Crea una tabla de frecuencias con intervalos de clase}
\usage{
tabla_cuantitativa(
  datos,
  k = 5,
  digitos = 2,
  metodo = "sturges",
  breaks = NULL,
  mostrar_tabla = TRUE,
  color_encabezado = "steelblue",
  color_total = "#F5F5F5",
  intervalo_cerrado = FALSE,
  labels = NULL,
  tabla_rayada = TRUE,
  tabla_hover = TRUE,
  ancho_completo = FALSE,
  mostrar_porcentaje = TRUE,
  mostrar_bordes = TRUE,
  formato_tabla = "html",
  nombres_columnas = NULL,
  titulo_tabla = NULL,
  pie_tabla = NULL
)
}
\arguments{
\item{datos}{Vector numérico con los datos a analizar}

\item{k}{Número de intervalos o clases deseados (por defecto 5)}

\item{digitos}{Número de decimales a mostrar en los resultados (por defecto 2)}

\item{metodo}{Método para calcular los breaks ("sturges", "fd", "scott" o "manual")}

\item{breaks}{Vector de puntos de corte (solo si metodo="manual")}

\item{mostrar_tabla}{Lógico. Si TRUE, muestra la tabla formateada}

\item{color_encabezado}{Color para el encabezado de la tabla (por defecto "steelblue")}

\item{color_total}{Color de fondo para la fila de totales si se incluye (por defecto "#F5F5F5")}

\item{intervalo_cerrado}{Lógico. Si TRUE, usa formato \link{a b} para intervalos cerrados en ambos extremos}

\item{labels}{Vector de caracteres con etiquetas personalizadas para los intervalos}

\item{tabla_rayada}{Lógico. Si TRUE, aplica estilo rayado a las filas de la tabla (por defecto TRUE)}

\item{tabla_hover}{Lógico. Si TRUE, aplica efecto hover a las filas (por defecto TRUE)}

\item{ancho_completo}{Lógico. Si TRUE, la tabla ocupa el ancho completo (por defecto FALSE)}

\item{mostrar_porcentaje}{Lógico. Si TRUE, muestra el símbolo \% en los porcentajes (por defecto TRUE)}

\item{mostrar_bordes}{Lógico. Si TRUE, muestra bordes entre columnas (por defecto TRUE)}

\item{formato_tabla}{Formato para la tabla: "html", "markdown", "latex" o "simple" (por defecto "html")}

\item{nombres_columnas}{Vector con nombres personalizados para las columnas (por defecto NULL)}

\item{titulo_tabla}{Título para la tabla (por defecto NULL, no muestra título)}

\item{pie_tabla}{Nota al pie de la tabla (por defecto NULL, no muestra pie)}
}
\value{
Un objeto de tabla formateado (kable) o una lista con los datos de la tabla y los puntos de corte
}
\description{
Esta función genera una tabla de frecuencias completa para una variable continua,
incluyendo frecuencias absolutas, relativas, acumuladas y marcas de clase.
}
\examples{
# Datos de ejemplo
datos_ejemplo <- rnorm(100, mean = 50, sd = 10)

# Tabla básica con 5 intervalos
tabla_cuantitativa(datos_ejemplo)

# Tabla con 7 intervalos y 3 decimales
tabla_cuantitativa(datos_ejemplo, k = 7, digitos = 3)

# Usar método de Freedman-Diaconis para calcular intervalos
tabla_cuantitativa(datos_ejemplo, metodo = "fd")

# Definir puntos de corte manualmente
tabla_cuantitativa(datos_ejemplo, metodo = "manual",
                   breaks = c(20, 30, 40, 50, 60, 70, 80))

# Personalizar aspecto
tabla_cuantitativa(datos_ejemplo, color_encabezado = "darkblue",
                   mostrar_porcentaje = TRUE)

}
