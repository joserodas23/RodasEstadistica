% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aiken.R
\name{aiken_v}
\alias{aiken_v}
\title{V de Aiken para validez de contenido}
\usage{
aiken_v(x, lo = 1, hi = 4, na.rm = TRUE)
}
\arguments{
\item{x}{Numeric. Vector de calificaciones (p. ej., 1–4). Se ignoran \code{NA} si \code{na.rm=TRUE}.}

\item{lo, hi}{Números mínimo y máximo de la escala (por defecto 1 y 4).}

\item{na.rm}{Logical. Si \code{TRUE}, elimina \code{NA} antes de calcular.}

\item{ci}{Logical. Si \code{TRUE}, devuelve intervalo de confianza (bootstrap).}

\item{R}{Integer. Réplicas bootstrap (default 2000).}

\item{conf}{Numeric. Nivel de confianza (default 0.95).}

\item{seed}{Integer o \code{NULL}. Semilla para reproducibilidad del bootstrap.}
}
\value{
\itemize{
\item Si \code{ci = FALSE}: un número (V).
\item Si \code{ci = TRUE}: una lista con \code{V}, \code{lower}, \code{upper}, \code{R}.
}
}
\description{
Calcula el índice \strong{V de Aiken} para un vector de calificaciones en una escala
discreta \code{lo}–\code{hi}. Opcionalmente estima \strong{intervalos de confianza}
por bootstrap percentil.
}
\details{
Fórmula (Aiken, 1985): \deqn{V = \frac{\sum (x - lo)}{n \,(hi - lo)}}
}
\examples{
aiken_v(c(4,4,3,3,4), lo = 1, hi = 4)
aiken_v(c(4,4,3,3,4), lo = 1, hi = 4, ci = TRUE, R = 1000, seed = 123)
}
